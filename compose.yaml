services:
  httpd:
    image: busybox
    ports:
      - "${HTTP_PORT}:80"
    volumes:
      - ./mta-sts.txt:/www/.well-known/mta-sts.txt:ro
    command: ["httpd", "-f", "-p", "80", "-h", "/www"]
    restart: unless-stopped
